1. xpiパッケージの生成
sh build_extension.sh -pv 0.0.4

2. 生成したxpiパッケージはMacの場合だと、Desktopに保存されます。（詳しくは汚いbuild_extensionを参考に）

3. Firefoxにドラッグアンドドロップでインストール

4. 拡張機能インストール後のボタンの表示の仕方
表示＞ツールバー＞カスタマイズからmixiチェックボタンのアイコンをツールバーにドラッグする。
